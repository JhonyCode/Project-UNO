const express = require('express');
const router = express.Router();

// Aquí van tus endpoints para las cartas
router.get('/', (req, res) => {
  // Lógica para obtener cartas
});

module.exports = router;
